//reverse string
str="navi" 
let str2 = "";
    for (let i = str.length - 1; i >= 0; i--) {
        str2 += str[i];
     }
   console.log(str2);

   //reverse number
    
   num="12345"
   num1="";
   for(i=num.length-1;i>=0;i--)
   {
     num1+=num[i]
   }
   console.log(num1)
